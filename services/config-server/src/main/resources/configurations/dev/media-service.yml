image-upload-dir: uploads

servlet:
  multipart:
    max-file-size: 5MB
    max-request-size: 5MB

# Local storage configuration
local-storage:
  base-path: uploads
  base-url: http://localhost:8006/api/v1/gcs/files

server:
  port: 8006